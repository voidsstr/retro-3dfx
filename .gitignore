# Wine-hosted build toolchain — large, fully reconstructable from public
# downloads; see toolchain-3dfx/README.md for how each piece is obtained.
toolchain-3dfx/bin/
toolchain-3dfx/wine/
toolchain-3dfx/prefix/
toolchain-3dfx/devtools/
toolchain-3dfx/downloads/
toolchain-3dfx/extract/

# The vintage source drop under "3dfx Driver Code/" is archived verbatim —
# add no ignore rules that could silently drop files from it.
