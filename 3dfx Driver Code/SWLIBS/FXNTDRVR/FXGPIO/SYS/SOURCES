# The sources for the Generic Port I/O device driver:

TARGETNAME=fxgpio
TARGETTYPE=DRIVER

# tweak the TARGETNAME so the sys file actually ends up in the obj\i386 directory
TARGETPATH=obj
TARGETNAME=..\$(TARGETNAME)

INCLUDES=.;$(DDKROOT)\incs

USER_C_FLAGS = /FAsc /Fa$O\$(@B).cod
LINKER_FLAGS = $(LINKER_FLAGS) -map:$O\$(@B).map

SOURCES=fxgpio.c fxgpio.rc
