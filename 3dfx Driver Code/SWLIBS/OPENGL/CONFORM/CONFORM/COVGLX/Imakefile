OBJS = enum.o shell.o test.o

INCLUDES = -I$(TOP)/include \
	   -I$(TOP)/include/GL

DEP_LIBS = $(TOP)/lib/GL/libGL.a

LOCAL_LIBS = $(DEP_LIBS) $(XLIB)

AllTarget(covglx)

NormalProgramTarget(covglx, $(OBJS), $(DEP_LIBS), $(LOCAL_LIBS), -lm)

DependTarget()
CleanTarget()
