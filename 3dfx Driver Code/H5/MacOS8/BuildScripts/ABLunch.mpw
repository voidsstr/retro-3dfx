######		ABLUNCH######	synopsis :###		ABLunch.mpw versionfilename###### Set the directory to the script folderSet		ablDir		`Directory`Directory	"{ablDir}scripts:"(Execute "Automated_Build.mpw" "{1}")### Set back the original dirDirectory {ablDir}