#pragma once/* The all-important private data structure for draw contexts. */#define VARI_VERT_LAYOUT		1		// allows use of grVertexLayout( disable ...#define	CUSTOM_GLIDE_STATE_DEF 	0		// as of 5-Jul-2000, couldn't get this to work right.#define ALLOW_GLIDE_FULLSCREEN  1		// we started dev of V3/V4/V5 driver in windowed mode only#if CUSTOM_GLIDE_STATE_DEF		#define kMaxAlphaTriStates		30   //  custom data size is much smaller; DV is the pig#else#define kMaxAlphaTriStates		30		// was 230;   // was 30; 29 in Bugdom; 17 in Nanosaur #endif#define kMaxAlphaTriInCache		2500  //  sizes array in drawPrivate for storing transparent triangles/* * vertex buffer. 33, 99, 999, and 3333 were tested at 16- & 32-bpp, and the lower, the faster (?) */#define			VERTARRAYBUFSIZ		33